@using System.Web.Optimization
@using Ornament
@using Ornament.Web
@{
    var showMenu = OrnamentContext.MemberShip.CurrentUser() != null || this.Response.StatusCode != 200;
}
<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/xhtml">
<head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewBag.Title</title>

    <link href='http://fonts.googleapis.com/css?family=Open+Sans:400,600,700' rel='stylesheet' type='text/css' />
    @*
    Backend admin templates.
    *@
    <link href="@Url.Content("~/content/templates/pannonia/css/main.css")" rel="stylesheet" type="text/css" />
    <!--[if IE 8]><link href="@Url.Content("~/content/templates/pannonia/css/ie8.css")" rel="stylesheet" type="text/css" /><![endif]-->

    <script type="text/javascript" src="/scripts/seajs/sea.js"> </script>
    <script src="/Scripts/config.js" type="text/javascript"></script>
    @RenderSection("head", false)
</head>
<body @Html.Raw(showMenu ? "" : "class=no-background")>
    <div id="top">
        <div class="fixed">
            <a href="@Url.Action("Index", "Home", new { area = "" })">Home</a>
            @Html.Partial("/Views/Shared/Backend/_topMenu.cshtml")
        </div>
    </div>
    @if (showMenu)
    {
        <div id="container">
            <div id="sidebar">
                @Html.Partial("/Views/Shared/Backend/" + OrnamentContext.Configuration.TemplateName() + "/_mainMenu.cshtml")
                @RenderSection("stuff", false)
            </div>

            <div id="content">
                <div class="wrapper">
                    @Html.Partial("/Views/Shared/Backend/" + OrnamentContext.Configuration.TemplateName() + "/_crumbs.cshtml")
                    <div class="page-header">
                        <div class="page-title">
                            @RenderSection("pageTitle", false)
                        </div>
                        @if (IsSectionDefined("page-stats"))
                        {
                            @RenderSection("page-stats", false)
                        }
                    </div>
                    @RenderBody()
                </div>
            </div>
        </div>
    }
    else
    {
        @RenderBody()
    }

    <div id="footer"></div>


    @if (!IsSectionDefined("scripts"))
    {
        <script type="text/javascript">
            seajs.use("/_applayout.js")
        </script>
    }


    @RenderSection("scripts", false)
</body>
</html>
