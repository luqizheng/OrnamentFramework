

<input id="loginid" type="text" class="box-filter" />
<button type="button" id="box1Clear" class="filter">x</button>
@*    <a id="searchUser" class="btn"><i class="icon-search"></i></a>*@

<select id="userSearcher" multiple="multiple" class="multiple">
</select>
<script type="text/javascript">
    var page = 0;
    var searchContent = "";

    require(['jquery'], function () {

        $("#userSearcher").endlessScroll({
            fireOnce: true,
            fireDelay: false,
            loader: "<div class=\"loading\"><div>",
            callback: function () {
                page++;
                InvokeSearch();
            }
        });

        $("#searchUser").click(function () {
            searchContent = $("#loginid").val();
            page = 0;
            InvokeSearch();
        });
        function InvokeSearch() {
            var data = {
                loginId: searchContent,
                page: page
            };
            $.getJSON('@this.Url.Action("Search", "User", new { area = "MemberShips" })', data, AddToSelector);
    }

    });


</script>
